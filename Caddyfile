{
    email {$ACME_EMAIL}
    auto_https off
}

http://actionlog.ru, http://www.actionlog.ru {
    encode gzip

    @api path /api/*
    handle @api {
        reverse_proxy backend:3001
    }

    handle {
        reverse_proxy frontend:80
    }
}
