{
    email {$ACME_EMAIL}
}

{$APP_DOMAIN:actionlog.ru}, www.{$APP_DOMAIN:actionlog.ru} {
    encode gzip

    @api path /api/*
    handle @api {
        reverse_proxy backend:3001
    }

    handle {
        reverse_proxy frontend:80
    }
}
