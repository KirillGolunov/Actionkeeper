<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>{{appName}} Magic Login Link</title>
    <style>
      body {
        font-family: 'Segoe UI', Arial, sans-serif;
        background: #F7F8FA;
        color: #222;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 480px;
        margin: 40px auto;
        background: #fff;
        border-radius: 10px;
        box-shadow: 0 2px 8px rgba(86,115,220,0.07);
        padding: 32px 32px 24px 32px;
      }
      .header {
        font-size: 22px;
        font-weight: 700;
        color: #5673DC;
        margin-bottom: 18px;
        text-align: center;
      }
      .cta {
        display: block;
        width: 100%;
        text-align: center;
        margin: 32px 0 24px 0;
      }
      .cta-button {
        background: #5673DC;
        color: #fff !important;
        padding: 14px 32px;
        border-radius: 6px;
        font-size: 17px;
        font-weight: 600;
        text-decoration: none;
        display: inline-block;
        margin: 0 auto;
      }
      .footer {
        font-size: 13px;
        color: #888;
        margin-top: 32px;
        text-align: center;
      }
      .plain-text {
        margin-top: 40px;
        font-size: 12px;
        color: #aaa;
        border-top: 1px solid #eee;
        padding-top: 16px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">Sign in to {{appName}}</div>
      <div style="font-size: 16px; color: #333; text-align: center;">Click the button below to log in. This link expires in {{expiresIn}} minutes and can only be used once.</div>
      <div class="cta">
        <a href="{{magicLink}}" class="cta-button">Log in to {{appName}}</a>
      </div>
      <div style="font-size: 14px; color: #555; text-align: center;">If you did not request this email, you can safely ignore it.</div>
      <div class="footer">&copy; {{year}} {{appName}}</div>
      <div class="plain-text">
        <strong>Plain-text version:</strong><br>
        Log in: {{magicLink}}<br>
        This link expires in {{expiresIn}} minutes and can only be used once.
      </div>
    </div>
  </body>
</html> 